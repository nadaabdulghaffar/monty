#include "monty.h"
/**
 * main - Entry point
 * @argc: Number of argumants passsed
 * @argv: array string pointe to arguments
 * Return: 0 on success
*/

int main(int argc, char **argv)
{
	(void) argv;
	arguments_validation(argc);
	return (0);
}
